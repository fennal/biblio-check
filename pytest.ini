[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# The whole suite runs offline and deterministically (network calls are
# monkeypatched in conftest.py), so it is safe to run in CI with no API keys.
addopts = -q
